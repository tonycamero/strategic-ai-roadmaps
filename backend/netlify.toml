[build]
  functions = "netlify/functions"
  command = "npm ci && mkdir -p netlify/functions/data && cp -R node_modules/pdfkit/js/data/* netlify/functions/data/"


[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

[functions]
  node_bundler = "esbuild"
  included_files = [
    "netlify/functions/data/**",
    "node_modules/pdfkit/js/data/**"
  ]

[[headers]]
  for = "/api/*"
  [headers.values]
    Access-Control-Allow-Origin = "https://portal.strategicai.app"
    Access-Control-Allow-Headers = "Content-Type, Authorization"
    Access-Control-Allow-Methods = "GET, POST, PUT, PATCH, DELETE, OPTIONS"

# location: backend/netlify.toml


