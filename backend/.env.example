# ============================================================================
# Strategic AI Roadmaps - Environment Variables
# ============================================================================

# Server Configuration
PORT=3001
NODE_ENV=production  # development | production

# Database (Neon PostgreSQL)
DATABASE_URL=postgresql://user:password@host.neon.tech/database?sslmode=require

# JWT Authentication
JWT_SECRET=your-super-secret-jwt-key-minimum-32-characters-change-in-production
JWT_EXPIRES_IN=7d

# Frontend URL (for CORS, invite links, password reset)
FRONTEND_URL=https://yourdomain.com

# Email Service (Password Reset, Invites)
# Option 1: Resend
RESEND_API_KEY=re_xxxxxxxxxxxxx
FROM_EMAIL=hello@yourdomain.com

# Option 2: SendGrid (alternative)
# SENDGRID_API_KEY=SG.xxxxxxxxxxxxx
# SENDGRID_FROM_EMAIL=hello@yourdomain.com

# OpenAI - AI Agents & Diagnostics
OPENAI_API_KEY=sk-xxxxxxxxxxxxx
OPENAI_TRUSTAGENT_ASSISTANT_ID=asst_xxxxx
OPENAI_TRUSTAGENT_VECTOR_STORE_ID=vs_xxxxx
OPENAI_TRUSTAGENT_MODEL=gpt-4o-mini

# ============================================================================
# OPTIONAL: Debug & Monitoring
# ============================================================================

# Enable SQL query logging (auto-disabled in production unless set)
# ENABLE_SQL_LOGGING=true

# Sentry (Error Tracking)
# SENTRY_DSN=https://xxxxx@sentry.io/xxxxx

# ============================================================================
# SECURITY NOTES
# ============================================================================
# - NEVER commit .env to git (already in .gitignore)
# - Use strong, randomly generated JWT_SECRET (min 32 chars)
# - Rotate secrets periodically
# - Use environment-specific secrets for dev/staging/prod
