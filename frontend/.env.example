# Frontend Environment Variables

# API URL (defaults to localhost:3001 in dev)
VITE_API_URL=http://localhost:3001

# TrustAgent Mode
# Options: 'simulated' | 'live'
# - simulated: Uses hardcoded demo flows, no OpenAI calls
# - live: Calls real backend API with OpenAI assistant
VITE_TRUSTAGENT_MODE=live
